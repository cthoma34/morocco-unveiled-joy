import DestinationPage from "@/pages/DestinationPage";
import { defaultDestination } from "@/config/destinations";

const Index = () => {
  return <DestinationPage config={defaultDestination} />;
};

export default Index;
